# Override for diskless overlayfs root
# The root filesystem is already mounted correctly by dracut
[Service]
# Make remount operations non-fatal (overlayfs is already correctly mounted)
Type=oneshot
RemainAfterExit=yes
ExecStart=
ExecStart=-/usr/lib/systemd/systemd-remount-fs
SuccessExitStatus=0 1 32
